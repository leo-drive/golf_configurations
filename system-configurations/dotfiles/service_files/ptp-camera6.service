[Unit]
Description=Precision Time Protocol (PTP) service for camera6 (CAM_BR)
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/sbin/ptp4l -i enp101s0 -m -H -A
ExecStop=/usr/bin/pkill -f ptp4l
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target network-online.target
