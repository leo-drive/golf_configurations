[Unit]
Description=Precision Time Protocol (PTP) service for camera5 (CAM_B)
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/sbin/ptp4l -i enp102s0 -m -H -A
ExecStop=/usr/bin/pkill -f ptp4l
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target network-online.target
