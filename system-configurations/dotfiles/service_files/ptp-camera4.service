[Unit]
Description=Precision Time Protocol (PTP) service for camera4 (CAM_MR)
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/sbin/ptp4l -i enp103s0 -m -H -A
ExecStop=/usr/bin/pkill -f ptp4l
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target network-online.target
