[Unit]
Description=Precision Time Protocol (PTP) service for camera3 (CAM_FL)
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/sbin/ptp4l -i enp3s0 -m -H -A
ExecStop=/usr/bin/pkill -f ptp4l
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target network-online.target
